# VoxClone - 需求 3：文本转语音 - 语音生成

## 📝 需求描述

实现核心的文本转语音（TTS）功能：用户输入文本，选择语音，调整生成参数，调用 AI 模型生成音频，并支持实时播放和波形显示。

---

## 🎯 核心功能

### 3.1 文本输入
- 多行文本输入框，最大 5000 字符
- 实时字符计数和成本预估
- 支持快捷清空和示例文本

### 3.2 语音选择
- 展示所有可用语音（系统 + 自定义）
- 语音试听功能
- 最近使用的语音记忆

### 3.3 参数调节
- **Temperature** (0-2)：控制生成的随机性，值越高变化越多
- **Top P** (0-1)：核采样概率，控制词汇选择范围
- **Top K** (1-10000)：限制候选词数量
- **Repetition Penalty** (1-2)：重复惩罚，防止过度重复

### 3.4 音频生成与播放
- 调用 Chatterbox API 生成音频
- 波形可视化（wavesurfer.js）
- 播放/暂停/进度控制
- 下载音频文件

---

## 📂 核心文件

### 前端组件
```
src/features/text-to-speech/components/text-to-speech-form.tsx    # TTS 表单
src/features/text-to-speech/components/text-input-panel.tsx       # 文本输入面板
src/features/text-to-speech/components/voice-selector.tsx         # 语音选择器
src/features/text-to-speech/components/voice-preview-panel.tsx    # 波形预览面板
src/features/text-to-speech/components/settings-panel.tsx         # 参数设置面板
src/features/text-to-speech/components/generate-button.tsx        # 生成按钮
src/features/text-to-speech/views/text-to-speech-view.tsx         # 主视图
src/features/text-to-speech/contexts/tts-voices-context.tsx       # 语音上下文
```

### 后端 API
```
src/trpc/routers/generations.ts    # 生成接口（create, getAll, getById）
src/lib/chatterbox-client.ts       # Chatterbox API 客户端
src/lib/r2.ts                      # R2 存储（保存生成的音频）
```

### 工具库
```
src/features/text-to-speech/hooks/use-wavesurfer.ts    # 波形可视化封装
src/features/text-to-speech/data/constants.ts          # 常量定义（最大长度、成本）
src/features/text-to-speech/data/sliders.ts            # 滑块配置
```

---

## 🔧 技术实现

### 1. 数据模型

**`prisma/schema.prisma`**

```prisma
model Generation {
  id String @id @default(cuid())

  orgId String  # 所属组织

  voiceId String?
  voice   Voice? @relation(fields: [voiceId], references: [id], onDelete: SetNull)

  text              String    # 输入文本
  voiceName         String    # 语音名称快照（防止语音被删除）
  r2ObjectKey       String?   # 生成音频的 R2 路径

  # 生成参数
  temperature       Float     # 随机性 (0-2)
  topP              Float     # 核采样 (0-1)
  topK              Int       # 候选词 (1-10000)
  repetitionPenalty Float     # 重复惩罚 (1-2)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([orgId])
  @@index([voiceId])
}
```

### 2. TTS 表单

**`src/features/text-to-speech/components/text-to-speech-form.tsx`**

```tsx
"use client";

import { z } from "zod";
import { useForm } from "@tanstack/react-form";
import { useMutation } from "@tanstack/react-query";
import { useRouter } from "next/navigation";
import { toast } from "sonner";

import { useTRPC } from "@/trpc/client";
import { useCheckout } from "@/features/billing/hooks/use-checkout";

// 表单验证规则
const ttsFormSchema = z.object({
  text: z.string().min(1, "Please enter some text"),
  voiceId: z.string().min(1, "Please select a voice"),
  temperature: z.number().min(0).max(2).default(0.8),
  topP: z.number().min(0).max(1).default(0.95),
  topK: z.number().min(1).max(10000).default(1000),
  repetitionPenalty: z.number().min(1).max(2).default(1.2),
});

export type TTSFormValues = z.infer<typeof ttsFormSchema>;

export const defaultTTSValues: TTSFormValues = {
  text: "",
  voiceId: "",
  temperature: 0.8,
  topP: 0.95,
  topK: 1000,
  repetitionPenalty: 1.2,
};

export function TextToSpeechForm({ children, defaultValues }) {
  const trpc = useTRPC();
  const router = useRouter();
  const { checkout } = useCheckout();

  // 调用 tRPC mutation
  const createMutation = useMutation(
    trpc.generations.create.mutationOptions({})
  );

  const form = useForm({
    defaultValues: defaultValues ?? defaultTTSValues,
    validators: {
      onSubmit: ttsFormSchema,
    },
    onSubmit: async ({ value }) => {
      try {
        const data = await createMutation.mutateAsync({
          text: value.text.trim(),
          voiceId: value.voiceId,
          temperature: value.temperature,
          topP: value.topP,
          topK: value.topK,
          repetitionPenalty: value.repetitionPenalty,
        });

        toast.success("Audio generated successfully!");
        // 跳转到详情页
        router.push(`/text-to-speech/${data.id}`);
      } catch (error) {
        const message = error instanceof Error ? error.message : "Failed to generate audio";

        if (message === "SUBSCRIPTION_REQUIRED") {
          toast.error("Subscription required", {
            action: { label: "Subscribe", onClick: () => checkout() },
          });
        } else {
          toast.error(message);
        }
      }
    },
  });

  return <form.AppForm>{children}</form.AppForm>;
}
```

### 3. 文本输入面板

**`src/features/text-to-speech/components/text-input-panel.tsx`**

```tsx
"use client";

import { TEXT_MAX_LENGTH, COST_PER_UNIT } from "../data/constants";

export function TextInputPanel() {
  return (
    <form.Field name="text">
      {(field) => {
        const charCount = field.state.value.length;
        const cost = (charCount / 1000) * COST_PER_UNIT;

        return (
          <div className="flex flex-col gap-2 p-4 border-b">
            <div className="flex justify-between items-center">
              <label className="text-sm font-medium">Text</label>
              <span className="text-xs text-muted-foreground">
                {charCount.toLocaleString()} / {TEXT_MAX_LENGTH.toLocaleString()} characters
                ${cost.toFixed(4)}
              </span>
            </div>

            <textarea
              value={field.state.value}
              onChange={(e) => field.handleChange(e.target.value)}
              onBlur={field.handleBlur}
              maxLength={TEXT_MAX_LENGTH}
              placeholder="Enter your text here..."
              className="min-h-[200px] w-full resize-none border rounded-md p-3 focus:outline-none focus:ring-2"
            />

            {field.state.meta.errors.length > 0 && (
              <p className="text-sm text-destructive">
                {field.state.meta.errors[0]?.message}
              </p>
            )}
          </div>
        );
      }}
    </form.Field>
  );
}
```

### 4. 参数设置面板

**`src/features/text-to-speech/components/settings-panel.tsx`**

```tsx
"use client";

import { Slider } from "@/components/ui/slider";

// 滑块配置
const SLIDER_CONFIG = {
  temperature: {
    label: "Temperature",
    description: "Controls randomness. Higher values = more variation",
    min: 0,
    max: 2,
    step: 0.1,
  },
  topP: {
    label: "Top P",
    description: "Nucleus sampling probability",
    min: 0,
    max: 1,
    step: 0.05,
  },
  topK: {
    label: "Top K",
    description: "Maximum number of candidate tokens",
    min: 1,
    max: 10000,
    step: 1,
  },
  repetitionPenalty: {
    label: "Repetition Penalty",
    description: "Penalize repeated tokens",
    min: 1,
    max: 2,
    step: 0.05,
  },
};

export function SettingsPanel() {
  return (
    <div className="w-80 border-l p-4 space-y-6">
      <h3 className="font-semibold">Generation Settings</h3>

      {/* Temperature */}
      <form.Field name="temperature">
        {(field) => (
          <div className="space-y-2">
            <div className="flex justify-between">
              <label className="text-sm font-medium">
                {SLIDER_CONFIG.temperature.label}
              </label>
              <span className="text-xs text-muted-foreground">
                {field.state.value.toFixed(1)}
              </span>
            </div>
            <Slider
              value={[field.state.value]}
              min={SLIDER_CONFIG.temperature.min}
              max={SLIDER_CONFIG.temperature.max}
              step={SLIDER_CONFIG.temperature.step}
              onValueChange={([v]) => field.handleChange(v)}
            />
            <p className="text-xs text-muted-foreground">
              {SLIDER_CONFIG.temperature.description}
            </p>
          </div>
        )}
      </form.Field>

      {/* Top P */}
      <form.Field name="topP">
        {(field) => (
          <div className="space-y-2">
            <div className="flex justify-between">
              <label className="text-sm font-medium">
                {SLIDER_CONFIG.topP.label}
              </label>
              <span className="text-xs text-muted-foreground">
                {field.state.value.toFixed(2)}
              </span>
            </div>
            <Slider
              value={[field.state.value]}
              min={SLIDER_CONFIG.topP.min}
              max={SLIDER_CONFIG.topP.max}
              step={SLIDER_CONFIG.topP.step}
              onValueChange={([v]) => field.handleChange(v)}
            />
          </div>
        )}
      </form.Field>

      {/* Top K */}
      <form.Field name="topK">
        {(field) => (
          <div className="space-y-2">
            <div className="flex justify-between">
              <label className="text-sm font-medium">
                {SLIDER_CONFIG.topK.label}
              </label>
              <span className="text-xs text-muted-foreground">
                {field.state.value}
              </span>
            </div>
            <Slider
              value={[field.state.value]}
              min={SLIDER_CONFIG.topK.min}
              max={SLIDER_CONFIG.topK.max}
              step={SLIDER_CONFIG.topK.step}
              onValueChange={([v]) => field.handleChange(v)}
            />
          </div>
        )}
      </form.Field>

      {/* Repetition Penalty */}
      <form.Field name="repetitionPenalty">
        {(field) => (
          <div className="space-y-2">
            <div className="flex justify-between">
              <label className="text-sm font-medium">
                {SLIDER_CONFIG.repetitionPenalty.label}
              </label>
              <span className="text-xs text-muted-foreground">
                {field.state.value.toFixed(2)}
              </span>
            </div>
            <Slider
              value={[field.state.value]}
              min={SLIDER_CONFIG.repetitionPenalty.min}
              max={SLIDER_CONFIG.repetitionPenalty.max}
              step={SLIDER_CONFIG.repetitionPenalty.step}
              onValueChange={([v]) => field.handleChange(v)}
            />
          </div>
        )}
      </form.Field>

      <GenerateButton />
    </div>
  );
}
```

### 5. 生成按钮

**`src/features/text-to-speech/components/generate-button.tsx`**

```tsx
"use client";

import { useMutation } from "@tanstack/react-query";
import { useTRPC } from "@/trpc/client";

export function GenerateButton() {
  const trpc = useTRPC();

  const mutation = useMutation(
    trpc.generations.create.mutationOptions({})
  );

  return (
    <form.Submit>
      {(isSubmitting) => (
        <Button
          type="submit"
          disabled={isSubmitting || mutation.isPending}
          className="w-full"
        >
          {isSubmitting || mutation.isPending ? (
            <>
              <Spinner className="size-4" />
              Generating...
            </>
          ) : (
            <>
              <AudioLines className="size-4" />
              Generate Audio
            </>
          )}
        </Button>
      )}
    </form.Submit>
  );
}
```

### 6. tRPC 生成接口

**`src/trpc/routers/generations.ts`**

```ts
import { z } from "zod";
import { TRPCError } from "@trpc/server";
import { polar } from "@/lib/polar";
import { env } from "@/lib/env";
import { chatterbox } from "@/lib/chatterbox-client";
import { prisma } from "@/lib/db";
import { uploadAudio } from "@/lib/r2";
import { TEXT_MAX_LENGTH } from "@/features/text-to-speech/data/constants";
import { createTRPCRouter, orgProcedure } from "../init";

export const generationsRouter = createTRPCRouter({
  // 获取单个生成记录
  getById: orgProcedure
    .input(z.object({ id: z.string() }))
    .query(async ({ input, ctx }) => {
      const generation = await prisma.generation.findUnique({
        where: { id: input.id, orgId: ctx.orgId },
        omit: { orgId: true, r2ObjectKey: true },
      });

      if (!generation) {
        throw new TRPCError({ code: "NOT_FOUND" });
      }

      return {
        ...generation,
        audioUrl: `/api/audio/${generation.id}`,
      };
    }),

  // 获取所有生成记录
  getAll: orgProcedure.query(async ({ ctx }) => {
    const generations = await prisma.generation.findMany({
      where: { orgId: ctx.orgId },
      orderBy: { createdAt: "desc" },
      omit: { orgId: true, r2ObjectKey: true },
    });

    return generations;
  }),

  // 创建新的音频生成
  create: orgProcedure
    .input(
      z.object({
        text: z.string().min(1).max(TEXT_MAX_LENGTH),
        voiceId: z.string().min(1),
        temperature: z.number().min(0).max(2).default(0.8),
        topP: z.number().min(0).max(1).default(0.95),
        topK: z.number().min(1).max(10000).default(1000),
        repetitionPenalty: z.number().min(1).max(2).default(1.2),
      })
    )
    .mutation(async ({ input, ctx }) => {
      // 1. 检查订阅状态
      try {
        const customerState = await polar.customers.getStateExternal({
          externalId: ctx.orgId,
        });
        const hasActiveSubscription =
          (customerState.activeSubscriptions ?? []).length > 0;
        if (!hasActiveSubscription) {
          throw new TRPCError({
            code: "FORBIDDEN",
            message: "SUBSCRIPTION_REQUIRED",
          });
        }
      } catch (err) {
        if (err instanceof TRPCError) throw err;
        throw new TRPCError({
          code: "FORBIDDEN",
          message: "SUBSCRIPTION_REQUIRED",
        });
      }

      // 2. 获取语音信息
      const voice = await prisma.voice.findUnique({
        where: {
          id: input.voiceId,
          OR: [
            { variant: "SYSTEM" },
            { variant: "CUSTOM", orgId: ctx.orgId },
          ],
        },
        select: { id: true, name: true, r2ObjectKey: true },
      });

      if (!voice) {
        throw new TRPCError({ code: "NOT_FOUND", message: "Voice not found" });
      }

      if (!voice.r2ObjectKey) {
        throw new TRPCError({
          code: "PRECONDITION_FAILED",
          message: "Voice audio not available",
        });
      }

      // 3. 调用 Chatterbox API 生成音频
      const { data, error } = await chatterbox.POST("/generate", {
        body: {
          prompt: input.text,
          voice_key: voice.r2ObjectKey,
          temperature: input.temperature,
          top_p: input.topP,
          top_k: input.topK,
          repetition_penalty: input.repetitionPenalty,
          norm_loudness: true,
        },
        parseAs: "arrayBuffer",
      });

      if (error) {
        throw new TRPCError({
          code: "INTERNAL_SERVER_ERROR",
          message: "Failed to generate audio",
        });
      }

      if (!(data instanceof ArrayBuffer)) {
        throw new TRPCError({
          code: "INTERNAL_SERVER_ERROR",
          message: "Invalid audio response",
        });
      }

      const buffer = Buffer.from(data);
      let generationId: string | null = null;
      let r2ObjectKey: string | null = null;

      // 4. 创建生成记录并保存音频
      try {
        const generation = await prisma.generation.create({
          data: {
            orgId: ctx.orgId,
            text: input.text,
            voiceName: voice.name,
            voiceId: voice.id,
            temperature: input.temperature,
            topP: input.topP,
            topK: input.topK,
            repetitionPenalty: input.repetitionPenalty,
          },
          select: { id: true },
        });

        generationId = generation.id;
        r2ObjectKey = `generations/orgs/${ctx.orgId}/${generation.id}`;

        // 上传到 R2
        await uploadAudio({ buffer, key: r2ObjectKey });

        // 更新记录
        await prisma.generation.update({
          where: { id: generation.id },
          data: { r2ObjectKey },
        });

      } catch {
        // 失败回滚
        if (generationId) {
          await prisma.generation.delete({ where: { id: generationId } })
            .catch(() => {});
        }
        throw new TRPCError({
          code: "INTERNAL_SERVER_ERROR",
          message: "Failed to store generated audio",
        });
      }

      // 5. 上报用量事件到 Polar（异步）
      polar.events.ingest({
        events: [{
          name: env.POLAR_METER_TTS_GENERATION,
          externalCustomerId: ctx.orgId,
          metadata: { [env.POLAR_METER_TTS_PROPERTY]: input.text.length },
          timestamp: new Date(),
        }],
      }).catch(() => {});

      return { id: generationId! };
    }),
});
```

### 7. Chatterbox API 客户端

**`src/lib/chatterbox-client.ts`**

```ts
import createClient from "openapi-fetch";
import type { paths } from "@/types/chatterbox-api";
import { env } from "./env";

export const chatterbox = createClient<paths>({
  baseUrl: env.CHATTERBOX_API_URL,
  headers: {
    "x-api-key": env.CHATTERBOX_API_KEY,
  },
});
```

### 8. 波形可视化

**`src/features/text-to-speech/hooks/use-wavesurfer.ts`**

```tsx
"use client";

import { useEffect, useState, useRef } from "react";
import WaveSurfer from "wavesurfer.js";

export function useWaveSurfer(audioUrl: string) {
  const containerRef = useRef<HTMLDivElement>(null);
  const wavesurferRef = useRef<WaveSurfer | null>(null);
  const [isPlaying, setIsPlaying] = useState(false);
  const [isReady, setIsReady] = useState(false);

  useEffect(() => {
    if (!containerRef.current) return;

    const ws = WaveSurfer.create({
      container: containerRef.current,
      waveColor: "rgb(200, 200, 200)",
      progressColor: "rgb(100, 100, 200)",
      cursorColor: "rgb(50, 50, 150)",
      barWidth: 2,
      barGap: 3,
      height: 120,
    });

    ws.load(audioUrl);

    ws.on("ready", () => {
      setIsReady(true);
    });

    ws.on("play", () => {
      setIsPlaying(true);
    });

    ws.on("pause", () => {
      setIsPlaying(false);
    });

    ws.on("finish", () => {
      setIsPlaying(false);
    });

    wavesurferRef.current = ws;

    return () => {
      ws.destroy();
    };
  }, [audioUrl]);

  const playPause = () => {
    wavesurferRef.current?.playPause();
  };

  const stop = () => {
    wavesurferRef.current?.stop();
    setIsPlaying(false);
  };

  return {
    containerRef,
    isPlaying,
    isReady,
    playPause,
    stop,
  };
}
```

### 9. 音频播放 API

**`src/app/api/audio/[generationId]/route.ts`**

```ts
import { auth } from "@clerk/nextjs/server";
import { prisma } from "@/lib/db";
import { getSignedAudioUrl } from "@/lib/r2";

export async function GET(
  _request: Request,
  { params }: { params: Promise<{ generationId: string }> }
) {
  const { userId, orgId } = await auth();

  if (!userId || !orgId) {
    return new Response("Unauthorized", { status: 401 });
  }

  const { generationId } = await params;

  const generation = await prisma.generation.findUnique({
    where: { id: generationId, orgId },
  });

  if (!generation) {
    return new Response("Not found", { status: 404 });
  }

  if (!generation.r2ObjectKey) {
    return new Response("Audio is not available yet", { status: 409 });
  }

  // 获取 R2 签名 URL
  const signedUrl = await getSignedAudioUrl(generation.r2ObjectKey);

  // 代理音频流
  const audioResponse = await fetch(signedUrl);

  if (!audioResponse.ok) {
    return new Response("Failed to fetch audio", { status: 502 });
  }

  return new Response(audioResponse.body, {
    headers: {
      "Content-Type": "audio/wav",
      "Cache-Control": "private, max-age=3600",
    },
  });
}
```

---

## 🌐 环境变量

```env
# Chatterbox API
CHATTERBOX_API_URL=https://api.chatterbox.ai
CHATTERBOX_API_KEY=your_api_key

# Cloudflare R2
R2_ACCOUNT_ID=your_account_id
R2_ACCESS_KEY_ID=your_access_key
R2_SECRET_ACCESS_KEY=your_secret_key
R2_BUCKET_NAME=resonance-audio

# Polar（计费）
POLAR_ACCESS_TOKEN=your_token
POLAR_METER_TTS_GENERATION=tts_generation
POLAR_METER_TTS_PROPERTY=characters
```

---

## 📊 数据流

```
1. 用户输入文本（最大 5000 字符）
   ↓
2. 选择语音（系统/自定义）
   ↓
3. 调整生成参数（Temperature, Top P, Top K, Repetition Penalty）
   ↓
4. 点击生成按钮
   ↓
5. 前端表单验证（Zod）
   ↓
6. 调用 tRPC: generations.create()
   ↓
7. 后端验证：
   - 认证状态（orgId）
   - 订阅状态（Polar）
   - 语音存在且可访问
   ↓
8. 调用 Chatterbox API POST /generate
   ↓
9. 接收音频 Buffer
   ↓
10. 创建 Generation 记录
   ↓
11. 上传音频到 R2
   ↓
12. 更新记录（保存 r2ObjectKey）
   ↓
13. 上报用量事件到 Polar（字符数）
   ↓
14. 返回 generationId
   ↓
15. 前端跳转到详情页 /text-to-speech/{id}
   ↓
16. 加载波形，播放音频
```

---

## 🧪 测试要点

### 前端测试
- [ ] 文本输入和字符计数
- [ ] 成本预估显示
- [ ] 语音选择器功能
- [ ] 参数滑块调节
- [ ] 表单验证和错误提示
- [ ] 波形渲染和播放控制

### 后端测试
- [ ] 未认证用户 → 401
- [ ] 无订阅用户 → 403 (SUBSCRIPTION_REQUIRED)
- [ ] 语音不存在 → 404
- [ ] 文本超过 5000 字符 → 验证失败
- [ ] Chatterbox API 调用失败 → 500
- [ ] 成功生成音频

### 集成测试
- [ ] 完整生成流程
- [ ] 详情页播放音频
- [ ] 历史列表显示
- [ ] Polar 用量上报

---

## ⚠️ 注意事项

1. **订阅检查**：生成前必须验证订阅状态
2. **错误回滚**：生成或上传失败时删除数据库记录
3. **参数验证**：所有参数都有范围限制
4. **语音快照**：保存 voiceName 而不是仅依赖 voiceId（防止语音被删除）
5. **异步上报**：Polar 用量事件不应阻塞响应

---

## 🎯 实践任务

1. **基础任务**：运行项目，输入文本并生成一段音频
2. **进阶任务**：调整参数，对比不同参数下的生成效果
3. **挑战任务**：实现批量生成功能（一次生成多个语音版本）

---

**创建日期**：2026-04-11
**难度级别**：⭐⭐⭐⭐（中高级）
**预计耗时**：4-6 小时
**前置知识**：tRPC, React Query, 表单处理，第三方 API 集成

# VoxClone - 需求 7：音频播放与预览

## 📝 需求描述

实现音频播放功能，包括波形可视化、播放控制、试听预览。支持语音试听和生成音频播放两种场景。

---

## 🎯 核心功能

### 7.1 波形可视化
- 使用 wavesurfer.js 显示音频波形
- 自定义颜色和样式
- 响应式布局适配

### 7.2 播放控制
- 播放/暂停切换
- 进度条拖拽
- 播放完成自动停止

### 7.3 语音试听
- 语音卡片上的快速试听
- 加载状态显示
- 同时只能播放一个音频

### 7.4 生成预览
- 生成完成后自动播放
- 波形+ 控制面板
- 下载音频文件

---

## 📂 核心文件

### 前端组件
```
src/features/text-to-speech/components/voice-preview-panel.tsx     # 波形预览面板
src/features/text-to-speech/components/voice-preview-mobile.tsx    # 移动端预览
src/features/voices/components/voice-card.tsx                      # 语音卡片（试听按钮）
```

### Hooks
```
src/features/text-to-speech/hooks/use-wavesurfer.ts    # wavesurfer 封装
src/hooks/use-audio-playback.ts                        # 简单音频播放 Hook
```

### 后端 API
```
src/app/api/audio/[generationId]/route.ts  # 生成音频流
src/app/api/voices/[voiceId]/route.ts      # 语音试听流
```

---

## 🔧 技术实现

### 1. Wavesurfer Hook

**`src/features/text-to-speech/hooks/use-wavesurfer.ts`**

```tsx
"use client";

import { useEffect, useState, useRef } from "react";
import WaveSurfer from "wavesurfer.js";

interface UseWaveSurferOptions {
  waveColor?: string;
  progressColor?: string;
  cursorColor?: string;
  barWidth?: number;
  barGap?: number;
  height?: number;
}

export function useWaveSurfer(
  audioUrl: string,
  options: UseWaveSurferOptions = {}
) {
  const containerRef = useRef<HTMLDivElement>(null);
  const wavesurferRef = useRef<WaveSurfer | null>(null);
  const [isPlaying, setIsPlaying] = useState(false);
  const [isReady, setIsReady] = useState(false);
  const [currentTime, setCurrentTime] = useState(0);
  const [duration, setDuration] = useState(0);

  useEffect(() => {
    if (!containerRef.current) return;

    // 销毁旧实例
    if (wavesurferRef.current) {
      wavesurferRef.current.destroy();
    }

    const ws = WaveSurfer.create({
      container: containerRef.current,
      waveColor: options.waveColor ?? "rgb(200, 200, 200)",
      progressColor: options.progressColor ?? "rgb(100, 100, 200)",
      cursorColor: options.cursorColor ?? "rgb(50, 50, 150)",
      barWidth: options.barWidth ?? 2,
      barGap: options.barGap ?? 3,
      height: options.height ?? 120,
      cursorWidth: 1,
      normalize: true,
    });

    // 加载音频
    ws.load(audioUrl);

    // 事件监听
    ws.on("ready", () => {
      setIsReady(true);
      setDuration(ws.getDuration());
    });

    ws.on("play", () => setIsPlaying(true));
    ws.on("pause", () => setIsPlaying(false));
    ws.on("finish", () => setIsPlaying(false));

    ws.on("timeupdate", (currentTime) => {
      setCurrentTime(currentTime);
    });

    ws.on("interaction", () => {
      ws.play();
    });

    wavesurferRef.current = ws;

    return () => {
      ws.destroy();
    };
  }, [audioUrl, options]);

  // 播放/暂停
  const playPause = () => {
    wavesurferRef.current?.playPause();
  };

  // 停止
  const stop = () => {
    wavesurferRef.current?.stop();
    setIsPlaying(false);
    setCurrentTime(0);
  };

  // 跳转到指定时间
  const seekTo = (time: number) => {
    wavesurferRef.current?.seekTo(time / duration);
  };

  // 格式化时间
  const formatTime = (seconds: number) => {
    const mins = Math.floor(seconds / 60);
    const secs = Math.floor(seconds % 60);
    return `${mins}:${secs.toString().padStart(2, "0")}`;
  };

  return {
    containerRef,
    isPlaying,
    isReady,
    currentTime,
    duration,
    playPause,
    stop,
    seekTo,
    formatTime,
  };
}
```

---

### 2. 波形预览面板

**`src/features/text-to-speech/components/voice-preview-panel.tsx`**

```tsx
"use client";

import { Play, Pause, Download, Volume2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Slider } from "@/components/ui/slider";
import { useWaveSurfer } from "../hooks/use-wavesurfer";

interface VoicePreviewPanelProps {
  audioUrl: string;
  voice?: { id?: string; name: string };
  text?: string;
}

export function VoicePreviewPanel({
  audioUrl,
  voice,
  text,
}: VoicePreviewPanelProps) {
  const {
    containerRef,
    isPlaying,
    isReady,
    currentTime,
    duration,
    playPause,
    formatTime,
  } = useWaveSurfer(audioUrl);

  if (!audioUrl) {
    return (
      <div className="flex-1 flex items-center justify-center border-t">
        <div className="text-center text-muted-foreground">
          <Volume2 className="size-8 mx-auto mb-2 opacity-50" />
          <p className="text-sm">Generate audio to preview</p>
        </div>
      </div>
    );
  }

  return (
    <div className="flex-1 border-t p-4 space-y-4">
      {/* 语音信息 */}
      <div className="flex items-center justify-between">
        <div>
          <p className="text-sm font-medium">{voice?.name ?? "Unknown Voice"}</p>
          {text && (
            <p className="text-xs text-muted-foreground truncate max-w-md">
              {text}
            </p>
          )}
        </div>
        <Button variant="outline" size="sm">
          <Download className="size-4" />
          <span className="ml-2 text-xs">Download</span>
        </Button>
      </div>

      {/* 波形容器 */}
      <div
        ref={containerRef}
        className="w-full rounded-lg bg-muted/50 overflow-hidden"
      />

      {/* 播放控制 */}
      <div className="flex items-center gap-4">
        <Button
          variant="default"
          size="icon"
          onClick={playPause}
          disabled={!isReady}
        >
          {isPlaying ? (
            <Pause className="size-5" />
          ) : (
            <Play className="size-5" />
          )}
        </Button>

        {/* 时间显示 */}
        <div className="flex-1 flex items-center gap-2">
          <span className="text-xs text-muted-foreground w-10 text-right">
            {formatTime(currentTime)}
          </span>
          <div className="flex-1 h-1 bg-muted rounded-full overflow-hidden">
            <div
              className="h-full bg-primary transition-all"
              style={{ width: `${(currentTime / duration) * 100}%` }}
            />
          </div>
          <span className="text-xs text-muted-foreground w-10">
            {formatTime(duration)}
          </span>
        </div>
      </div>
    </div>
  );
}
```

---

### 3. 移动端预览

**`src/features/text-to-speech/components/voice-preview-mobile.tsx`**

```tsx
"use client";

import { Play, Pause } from "lucide-react";
import { Button } from "@/components/ui/button";
import { useWaveSurfer } from "../hooks/use-wavesurfer";

interface VoicePreviewMobileProps {
  audioUrl: string;
  voice?: { name: string };
  text?: string;
}

export function VoicePreviewMobile({
  audioUrl,
  voice,
  text,
}: VoicePreviewMobileProps) {
  const {
    containerRef,
    isPlaying,
    isReady,
    playPause,
  } = useWaveSurfer(audioUrl, { height: 80 });

  if (!audioUrl) return null;

  return (
    <div className="lg:hidden border-t p-4 space-y-3">
      <div className="flex items-center justify-between">
        <div className="min-w-0 flex-1">
          <p className="text-sm font-medium truncate">{voice?.name}</p>
          <p className="text-xs text-muted-foreground truncate">
            {text?.slice(0, 50)}...
          </p>
        </div>
        <Button
          variant="outline"
          size="icon"
          onClick={playPause}
          disabled={!isReady}
        >
          {isPlaying ? (
            <Pause className="size-4" />
          ) : (
            <Play className="size-4" />
          )}
        </Button>
      </div>

      <div
        ref={containerRef}
        className="w-full rounded bg-muted/50"
      />
    </div>
  );
}
```

---

### 4. 简单音频播放 Hook

**`src/hooks/use-audio-playback.ts`**

```tsx
"use client";

import { useState, useRef, useEffect } from "react";

export function useAudioPlayback(src: string) {
  const [isPlaying, setIsPlaying] = useState(false);
  const [isLoading, setIsLoading] = useState(false);
  const audioRef = useRef<HTMLAudioElement | null>(null);

  useEffect(() => {
    const audio = new Audio(src);

    audio.onplay = () => setIsPlaying(true);
    audio.onpause = () => setIsPlaying(false);
    audio.onended = () => setIsPlaying(false);
    audio.onwaiting = () => setIsLoading(true);
    audio.oncanplay = () => setIsLoading(false);
    audio.onerror = () => {
      setIsLoading(false);
      setIsPlaying(false);
    };

    audioRef.current = audio;

    return () => {
      audio.pause();
      audio.src = "";
      audio = null;
    };
  }, [src]);

  const togglePlay = () => {
    if (!audioRef.current) return;

    if (isPlaying) {
      audioRef.current.pause();
    } else {
      audioRef.current.play().catch(console.error);
    }
  };

  return { isPlaying, isLoading, togglePlay };
}
```

---

### 5. 语音卡片试听

**`src/features/voices/components/voice-card.tsx`（片段）**

```tsx
export function VoiceCard({ voice }: VoiceCardProps) {
  const audioSrc = `/api/voices/${encodeURIComponent(voice.id)}`;
  const { isPlaying, isLoading, togglePlay } = useAudioPlayback(audioSrc);

  return (
    <div className="flex items-center gap-1 overflow-hidden rounded-xl border pr-3">
      {/* ... 其他内容 ... */}

      <div className="ml-1 flex shrink-0 items-center gap-1">
        <Button
          variant="outline"
          size="icon-sm"
          className="rounded-full"
          onClick={togglePlay}
          disabled={isLoading}
        >
          {isLoading ? (
            <Spinner className="size-4" />
          ) : isPlaying ? (
            <Pause className="size-4" />
          ) : (
            <Play className="size-4" />
          )}
        </Button>
        {/* ... 更多操作 ... */}
      </div>
    </div>
  );
}
```

---

### 6. 音频流 API

**`src/app/api/voices/[voiceId]/route.ts`**

```ts
import { auth } from "@clerk/nextjs/server";
import { prisma } from "@/lib/db";
import { getSignedAudioUrl } from "@/lib/r2";

export async function GET(
  _request: Request,
  { params }: { params: Promise<{ voiceId: string }> }
) {
  const { userId, orgId } = await auth();

  if (!userId || !orgId) {
    return new Response("Unauthorized", { status: 401 });
  }

  const { voiceId } = await params;

  const voice = await prisma.voice.findUnique({
    where: { id: voiceId },
    select: {
      variant: true,
      orgId: true,
      r2ObjectKey: true,
    },
  });

  if (!voice) {
    return new Response("Not found", { status: 404 });
  }

  // 自定义语音必须属于当前组织
  if (voice.variant === "CUSTOM" && voice.orgId !== orgId) {
    return new Response("Not found", { status: 404 });
  }

  if (!voice.r2ObjectKey) {
    return new Response("Voice audio is not available yet", { status: 409 });
  }

  const signedUrl = await getSignedAudioUrl(voice.r2ObjectKey);
  const audioResponse = await fetch(signedUrl);

  if (!audioResponse.ok) {
    return new Response("Failed to fetch voice audio", { status: 502 });
  }

  const contentType = audioResponse.headers.get("content-type") || "audio/wav";

  return new Response(audioResponse.body, {
    headers: {
      "Content-Type": contentType,
      "Cache-Control":
        voice.variant === "SYSTEM"
          ? "public, max-age=86400"  // 系统语音：公共缓存 1 天
          : "private, max-age=3600", // 自定义语音：私有缓存 1 小时
    },
  });
}
```

---

## 🌐 Wavesurfer.js 配置

### 安装

```bash
npm install wavesurfer.js
```

### 基础配置

```ts
WaveSurfer.create({
  container: "#waveform",
  waveColor: "rgb(200, 200, 200)",
  progressColor: "rgb(100, 100, 200)",
  cursorColor: "rgb(50, 50, 150)",
  barWidth: 2,
  barGap: 3,
  height: 120,
  cursorWidth: 1,
  normalize: true,  // 标准化音量
});
```

### 高级插件

```ts
import RegionsPlugin from 'wavesurfer.js/dist/plugins/regions.esm.js';
import TimelinePlugin from 'wavesurfer.js/dist/plugins/timeline.esm.js';

const wavesurfer = WaveSurfer.create({
  // ... 基础配置
  plugins: [
    RegionsPlugin.create(),
    TimelinePlugin.create(),
  ],
});
```

---

## 🧪 测试要点

### 功能测试
- [ ] 波形正确渲染
- [ ] 播放/暂停切换
- [ ] 进度条更新
- [ ] 拖拽跳转进度
- [ ] 播放完成自动停止
- [ ] 下载音频文件

### 兼容性测试
- [ ] Chrome / Firefox / Safari
- [ ] 移动端响应式
- [ ] 触摸设备支持

### 性能测试
- [ ] 大文件加载
- [ ] 多音频同时播放
- [ ] 内存泄漏检查

---

## ⚠️ 注意事项

1. **单一实例**：确保同时只有一个音频在播放
2. **资源清理**：组件卸载时销毁 wavesurfer 实例
3. **错误处理**：音频加载失败的优雅降级
4. **缓存策略**：系统语音和自定义语音不同缓存时间
5. **移动端**：自动播放受限，需要用户交互

---

## 🎯 实践任务

1. **基础任务**：实现语音卡片试听功能
2. **进阶任务**：自定义波形颜色和样式
3. **挑战任务**：实现音频片段循环播放功能

---

**创建日期**：2026-04-11
**难度级别**：⭐⭐⭐（中级）
**预计耗时**：3-5 小时
**前置知识**：Web Audio API, React Hooks, Canvas
